tr.align-middle
  td
    = image_tag tour.thumbnail_url, width: 130, class: 'img-fluid rounded'
  td
    = tour.title
  td
    = tour.category
  td
    = tour.status == 1 ? 'Hiện' : 'Ẩn'
  td
    = tour.created_at.in_time_zone("Hanoi").strftime("%H:%M %d-%m-%Y")
  td
    =link_to users_tour_path(tour), class: 'btn btn-warning btn-sm mr-3' do
      i.far.fa-edit

    =link_to users_tour_path(tour), class: 'btn btn-danger btn-sm', data: { turbo_method: :delete, turbo_confirm: 'Bạn chắn muốn xoá' }  do
      i.far.fa-trash-alt
